name: Build geoip files
on:
  workflow_dispatch:
  schedule:
    - cron: "0 0 * * 4"
  push:
    branches:
      - master
    paths-ignore:
      - "assets/*"
      - ".gitignore"
      - "LICENSE*"
      - "*.md"
  pull_request:
    branches:
      - master
    paths-ignore:
      - "assets/*"
      - ".gitignore"
      - "LICENSE*"
      - "*.md"
permissions:
  contents: write
jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout codebase
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Set up Go
        uses: actions/setup-go@v7
        with:
          go-version-file: ./go.mod
          cache-dependency-path: ./go.sum

      - name: Set variables
        run: |
          echo "TAG_NAME=$(date +%Y%m%d%H%M)" >> $GITHUB_ENV
          echo "RELEASE_NAME=$(date +%Y%m%d%H%M)" >> $GITHUB_ENV
          echo "YEAR=$(date +%Y)" >> $GITHUB_ENV
          echo "MONTH=$(date +%m)" >> $GITHUB_ENV
        shell: bash

      # ----- 只下载 MaxMind ASN 数据（保留 ASN 功能） -----
      - name: Download GeoLite2 ASN databases
        env:
          LICENSE_KEY: ${{ secrets.MAXMIND_GEOLITE2_LICENSE }}
        run: |
          mkdir -p output
          curl -L "https://download.maxmind.com/app/geoip_download?edition_id=GeoLite2-ASN&license_key=${LICENSE_KEY}&suffix=tar.gz" -o ./output/GeoLite2-ASN.tar.gz
          curl -L "https://download.maxmind.com/app/geoip_download?edition_id=GeoLite2-ASN-CSV&license_key=${LICENSE_KEY}&suffix=zip" -o ./output/GeoLite2-ASN-CSV.zip

      # ----- 下载 DB-IP Country Lite MMDB（替代 MaxMind Country） -----
      - name: Download DB-IP Country Lite database
        run: |
          mkdir -p db-ip
          curl -L -o db-ip/dbip-country-lite.mmdb.gz "https://download.db-ip.com/free/dbip-country-lite-${{ env.YEAR }}-${{ env.MONTH }}.mmdb.gz"
          gzip -d db-ip/dbip-country-lite.mmdb.gz
          # 复制一份到 output 供 maxmindMMDB 输出使用
          cp db-ip/dbip-country-lite.mmdb ./output/GeoLite2-Country.mmdb

      # ----- 解压 ASN 数据（不再需要 MaxMind Country） -----
      - name: Prepare GeoLite2 ASN databases
        run: |
          cp ./output/*.tar.gz ./
          cp ./output/*.zip ./
          unzip GeoLite2-ASN-CSV.zip
          tar -xvzf GeoLite2-ASN.tar.gz
          # 复制 ASN CSV 到 geolite2 目录（供 ASN 输入使用）
          mkdir -p geolite2
          cp GeoLite2-ASN-CSV_*/*.csv ./geolite2/
          # 注意：不再有 Country CSV

      # ----- 修改 config.json：将国家数据源替换为 dbipCountryMMDB -----
      - name: Update config.json to use DB-IP
        run: |
          # 使用 jq 替换 input 中类型为 maxmindGeoLite2CountryCSV 的条目
          # 先删除该条目，再添加新条目（保持位置大致不变）
          jq '.input |= map(if .type=="maxmindGeoLite2CountryCSV" then empty else . end)' config.json > config.tmp.json
          # 在 input 数组开头插入新的 dbipCountryMMDB 条目
          jq '.input = [{"type": "dbipCountryMMDB", "action": "add", "args": {"uri": "./db-ip/dbip-country-lite.mmdb"}}] + .input' config.tmp.json > config.json
          # 清理
          rm config.tmp.json

      # ----- 以下步骤完全不变 -----
      - name: Clean ChinaMax_IP.txt
        run: |
          curl -sSL https://raw.githubusercontent.com/blackmatrix7/ios_rule_script/master/rule/Clash/ChinaMax/ChinaMax_IP.txt \
            | grep -v '^#' \
            | grep -v '^DOMAIN:' \
            | grep -v '^IP-ASN:' \
            | grep -v '^TOTAL:' \
            | grep -v '^PROCESS-NAME:' \
            | grep -v '^IP-CIDR:' \
            | grep -v '^IP-CIDR6:' \
            | grep -v '^[[:space:]]*$' \
            | sed -E 's/^IP-CIDR,//; s/^IP-CIDR6,//' \
            | grep -v '^[0-9]\+$' \
            | grep -v '::ffff:' \
            > china_clean.txt

      - name: Build geoip files
        run: |
          go build ./
          ./geoip convert -c ./config.json

      - name: Verify mmdb files
        run: |
          cd ./output || exit 1
          go install -v github.com/maxmind/mmdbverify@latest
          for name in $(ls *.mmdb); do
            $(go env GOPATH)/bin/mmdbverify -file ${name}
          done

      - name: Generate sha256 checksum for dat files
        run: |
          cd ./output || exit 1
          for name in $(ls *.dat); do
            sha256sum ${name} > ./${name}.sha256sum
          done

      - name: Generate sha256 checksum for mmdb files
        run: |
          cd ./output || exit 1
          for name in $(ls *.mmdb); do
            sha256sum ${name} > ./${name}.sha256sum
          done

      - name: Git push assets to "release" branch
        if: github.event_name != 'pull_request'
        run: |
          cd output || exit 1
          git init
          git config --local user.name "github-actions[bot]"
          git config --local user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git checkout -b release
          git add -A
          git commit -m "${{ env.RELEASE_NAME }}"
          git remote add geoip "https://${{ github.actor }}:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}"
          git push -f -u geoip release

      - name: Purge jsdelivr CDN
        if: github.event_name != 'pull_request'
        run: |
          cd output || exit 1
          for file in $(ls); do
            curl -i "https://purge.jsdelivr.net/gh/${{ github.repository }}@release/${file}"
          done

      - name: Remove some files to avoid publishing to GitHub release
        if: github.event_name != 'pull_request'
        run: |
          rm -rf ./output/*.{gz,zip}
          rm -rf ./output/GeoLite2-*.csv
          rm -rf ./output/GeoLite2-*.mmdb
          rm -rf ./output/GeoLite2-*.sha256sum
          rm -rf ./output/{clash,dat,mrs,nginx,srs,surge,text}
          rm -rf ./db-ip   # 清理中间目录

      - name: Upload files to GitHub release
        if: github.event_name != 'pull_request'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release create ${{ env.TAG_NAME }} --title "${{ env.RELEASE_NAME }}" ./output/*
